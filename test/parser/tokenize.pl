% test for correct tokenizing

term('Test'):
  term(
    [ token(
        variable(
          [ layout_text_sequence([]),
            variable_token(
              named_variable(
                [ capital_letter_char('T'),
                  alphanumeric_char(
                    alpha_char(
                      letter_char(
                        small_letter_char(e)))),
                  alphanumeric_char(
                    alpha_char(
                      letter_char(
                        small_letter_char(s)))),
                  alphanumeric_char(
                    alpha_char(
                      letter_char(
                        small_letter_char(t))))
                ]))
          ]))
    ]).
